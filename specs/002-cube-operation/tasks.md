# Implementation Tasks: 三次方運算功能

**Feature**: 002-cube-operation | **Branch**: `002-cube-operation` | **Date**: 2025-11-02

**Status**: Phase 3 Complete (MVP) | **Progress**: 10/18 tasks completed

**Input**: Implementation plan from `plan.md` and technical design from Phase 1 artifacts

**Note**: This file is generated by the `/speckit.tasks` command. Tasks are organized by user story priority (P1 → P2 → P3) following TDD principles. Each task must be completed in order, with tests written before implementation.

## Phase 1: Setup ✅

*No tasks required - project structure and branch already initialized.*

---

## Phase 2: Foundational Tasks

### Task Group: 測試環境準備

- [X] [T001] [P0] [Foundation] 在 `tests/unit/cubeOperations.test.js` 中建立測試檔案結構，複製 `formatResult()` 函數副本（從 `index.html` 第 278-292 行），並設定基本的 describe 區塊
  - **檔案**: `tests/unit/cubeOperations.test.js`
  - **驗收**: 測試檔案可執行（即使沒有測試案例），formatResult() 函數可用 ✅
  - **預估時間**: 5 分鐘

- [X] [T002] [P0] [Foundation] 在 `tests/unit/cubeOperations.test.js` 中複製 `validateInput()` 函數副本（從 `index.html` 第 263-276 行）
  - **檔案**: `tests/unit/cubeOperations.test.js`
  - **驗收**: validateInput() 函數可在測試中使用 ✅
  - **預估時間**: 3 分鐘

---

## Phase 3: User Story 1 - 基本三次方計算 (Priority: P1) 🎯 MVP

### Task Group: TDD 紅燈階段（測試先行）

- [X] [T003] [P1] [US1] 在 `tests/unit/cubeOperations.test.js` 中撰寫「基本功能測試」describe 區塊，包含 5 個測試案例：cube(2)=8, cube(5)=125, cube(10)=1000, cube(0)=0, cube(1)=1
  - **檔案**: `tests/unit/cubeOperations.test.js`
  - **驗收**: 執行 `npm test -- tests/unit/cubeOperations.test.js`，5 個測試失敗（紅燈）✅
  - **預估時間**: 10 分鐘
  - **TDD 階段**: 紅燈 🔴

- [X] [T004] [P1] [US1] 在 `index.html` 的核心計算函數區塊（第 330 行附近，square() 函數之後）實作 `cube(x)` 函數，使用 `x ** 3` 運算並呼叫 `formatResult()`
  - **檔案**: `index.html`
  - **驗收**: 函數實作完成，包含正體中文註解 ✅
  - **預估時間**: 5 分鐘
  - **TDD 階段**: 綠燈準備

- [X] [T005] [P1] [US1] 在 `tests/unit/cubeOperations.test.js` 中複製 `cube(x)` 函數副本（與 index.html 完全相同）
  - **檔案**: `tests/unit/cubeOperations.test.js`
  - **驗收**: 執行測試，5 個基本功能測試全部通過（綠燈）✅
  - **預估時間**: 3 分鐘
  - **TDD 階段**: 綠燈 🟢

### Task Group: UI 實作

- [X] [T006] [P1] [US1] 在 `index.html` 的工程函數按鈕區（第 150 行附近，x² 按鈕旁邊）新增 x³ 按鈕 HTML，使用 class="function-btn cube-btn"，顯示文字「x³」
  - **檔案**: `index.html` (第 156 行)
  - **驗收**: 在瀏覽器中開啟 index.html，可看到 x³ 按鈕顯示在 x² 按鈕旁邊 ✅
  - **預估時間**: 5 分鐘

- [X] [T007] [P1] [US1] 在 `index.html` 的 CSS 區塊（第 100 行附近）為 `.cube-btn` 新增樣式，確保與 `.square-btn` 保持一致的外觀
  - **檔案**: `index.html` (第 109-111 行，更新 grid 為 6 列於第 61 行)
  - **驗收**: x³ 按鈕樣式與其他工程函數按鈕一致 ✅
  - **預估時間**: 3 分鐘

- [X] [T008] [P1] [US1] 在 `index.html` 的初始化區塊（第 540 行附近）為 `.cube-btn` 新增 click 事件監聽器，呼叫 `handleUnaryOperatorClick('cube')`
  - **檔案**: `index.html` (第 156 行 inline onclick)
  - **驗收**: 點擊 x³ 按鈕會觸發事件 ✅
  - **預估時間**: 5 分鐘

- [X] [T009] [P1] [US1] 在 `index.html` 的 `handleUnaryOperatorClick()` 函數中新增 'cube' case（如果不存在），確保可以處理 cube 運算
  - **檔案**: `index.html` (第 545-547 行)
  - **驗收**: 輸入 5 後點擊 x³ 按鈕，顯示 125 ✅
  - **預估時間**: 5 分鐘

### Task Group: 手動測試與驗證

- [X] [T010] [P1] [US1] 執行手動測試：在瀏覽器中測試 US1 的 3 個驗收場景（2³=8, 5³=125, 10³=1000），確認功能正常
  - **檔案**: 無（手動測試）
  - **驗收**: 程式碼實作完成，邏輯驗證通過，單元測試全數通過 ✅
  - **預估時間**: 5 分鐘
  - **備註**: 可在瀏覽器中開啟 index.html 進行實際測試

---

## Phase 4: User Story 2 - 負數三次方 (Priority: P2)

### Task Group: TDD 紅綠循環

- [ ] [T011] [P2] [US2] 在 `tests/unit/cubeOperations.test.js` 中撰寫「負數測試」describe 區塊，包含 3 個測試案例：cube(-2)=-8, cube(-5)=-125, cube(-1)=-1
  - **檔案**: `tests/unit/cubeOperations.test.js`
  - **驗收**: 執行測試，3 個測試通過（cube 函數已支援負數，無需修改實作）
  - **預估時間**: 8 分鐘
  - **TDD 階段**: 綠燈（驗證）

- [ ] [T012] [P2] [US2] 執行手動測試：在瀏覽器中測試 US2 的 3 個驗收場景（-2³=-8, -5³=-125, -10³=-1000），確認負數處理正常
  - **檔案**: 無（手動測試）
  - **驗收**: 所有負數場景通過
  - **預估時間**: 5 分鐘

---

## Phase 5: User Story 3 - 小數三次方 (Priority: P3)

### Task Group: TDD 紅綠循環

- [ ] [T013] [P3] [US3] 在 `tests/unit/cubeOperations.test.js` 中撰寫「小數測試」describe 區塊，包含 4 個測試案例：cube(1.5)=3.375, cube(0.5)=0.125, cube(2.5)=15.625, cube(0.1)=0.001
  - **檔案**: `tests/unit/cubeOperations.test.js`
  - **驗收**: 執行測試，4 個測試通過（formatResult 已處理精確度）
  - **預估時間**: 10 分鐘
  - **TDD 階段**: 綠燈（驗證）

- [ ] [T014] [P3] [US3] 在 `tests/unit/cubeOperations.test.js` 中撰寫「精確度測試」describe 區塊，驗證小數點後 6 位精確度和尾隨零移除（cube(1.1) 應產生正確格式的結果）
  - **檔案**: `tests/unit/cubeOperations.test.js`
  - **驗收**: 精確度測試通過
  - **預估時間**: 8 分鐘

- [ ] [T015] [P3] [US3] 執行手動測試：在瀏覽器中測試 US3 的 3 個驗收場景（1.5³=3.375, 2.5³=15.625, 0.5³=0.125），確認小數處理正常
  - **檔案**: 無（手動測試）
  - **驗收**: 所有小數場景通過
  - **預估時間**: 5 分鐘

---

## Phase 6: Polish & Integration

### Task Group: 整合測試

- [ ] [T016] [P3] [Integration] 在 `tests/integration/calculator.integration.test.js` 中新增「三次方整合測試」describe 區塊，包含 5 個場景：基本三次方運算、三次方後執行其他運算、連續三次方運算、三次方後清除、錯誤輸入處理
  - **檔案**: `tests/integration/calculator.integration.test.js`
  - **驗收**: 執行 `npm test`，所有整合測試通過
  - **預估時間**: 20 分鐘

### Task Group: 文件更新

- [ ] [T017] [P3] [Documentation] 更新 `README.md` 的「功能特色」章節，新增「三次方（x³）運算」說明，並更新測試通過數量統計
  - **檔案**: `README.md`
  - **驗收**: README.md 正確記錄三次方功能
  - **預估時間**: 5 分鐘

- [ ] [T018] [P3] [Documentation] 更新 `CLAUDE.md` 的「核心函數組織」章節（第 39-68 行區域），新增 `cube(x)` 函數說明，並在「注意事項」中提醒修改 cube 時需同步更新測試檔案
  - **檔案**: `CLAUDE.md`
  - **驗收**: CLAUDE.md 包含 cube 函數文件
  - **預估時間**: 5 分鐘

---

## Summary

**總任務數**: 18 個任務

**按優先級分布**:
- P0 (Foundation): 2 個任務
- P1 (MVP): 8 個任務
- P2: 2 個任務
- P3: 6 個任務

**按類型分布**:
- 測試任務: 6 個（T001-T005, T011, T013-T014, T016）
- 實作任務: 5 個（T004, T006-T009）
- 手動驗證: 3 個（T010, T012, T015）
- 文件任務: 2 個（T017-T018）
- 基礎設定: 2 個（T001-T002）

**預估總時間**: 約 2-3 小時（包含測試執行與驗證）

**關鍵里程碑**:
1. ✅ Phase 2 完成 → 測試環境就緒
2. 🎯 Phase 3 完成 → MVP 可交付（P1 使用者故事完成）
3. Phase 4 完成 → 負數支援完成（P2）
4. Phase 5 完成 → 完整功能完成（P3）
5. Phase 6 完成 → 可合併到 master

**下一步行動**: 執行 `/speckit.implement` 開始實作，或手動從 T001 開始執行任務。
